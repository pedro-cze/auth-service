server:
  port: 8989

management:
  server:
    port: 9999

spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/users
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083
