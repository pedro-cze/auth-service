spring:
  application:
    name: auth-service
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:users-test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  cloud:
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
